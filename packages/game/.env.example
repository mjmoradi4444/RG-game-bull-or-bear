# Frontend build-time config (Vite). Copy to .env.local to override.
# Base URL of the bot's score API (packages/bot). Used only by GamesPlatformAdapter;
# the plain-browser NoopAdapter ignores it. Leave unset for local dev.
VITE_SCORE_API=https://your-bot-host.example
